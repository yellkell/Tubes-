{
  "name": "tubes",
  "version": "0.1.0",
  "description": "TUBES — a passthrough-AR pipefitting toy-puzzle for Quest. The old machine behind your real walls wants back on: mount a flange, haul a two-handed industrial tube out of it, and seat the far end in a socket the room grew on another wall. Light pours through. Built on the Immersive Web SDK.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@iwsdk/core": "0.4.2",
    "three": "0.184.0"
  },
  "devDependencies": {
    "@iwsdk/vite-plugin-dev": "0.4.2",
    "@types/three": "0.184.0",
    "playwright": "^1.62.1",
    "typescript": "^5.6.0",
    "vite": "^7.0.0"
  }
}
